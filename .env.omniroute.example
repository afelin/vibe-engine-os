# Phase 2 OPTIONAL — OmniRoute gateway. Do NOT install by default.
# Only if direct Groq (Phase 1 / .env.experiment.example) is insufficient.
# Pin exact version; connect Groq + Gemini API keys only.
# Banned: Fusion, multi-model panels, aggressive/ultra compression, OAuth/web-cookie providers.
# Not required for orchestrator Phase 1 (direct Groq via router.ts).
# ANTHROPIC_BASE_URL=http://localhost:20128
# ANTHROPIC_AUTH_TOKEN=omniroute-local-token
# OMNIROUTE_VERSION=3.8.46
